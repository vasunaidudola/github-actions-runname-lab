name: PR Workflow - Clean Branch Name

on:
  pull_request:

run-name: Build and Deploy – Feature Branch ${{ github.ref_name }} (PR ${{ github.event.pull_request.number }})

jobs:
  build_and_deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Demo step
        run: echo "Build and Deploy job running"
